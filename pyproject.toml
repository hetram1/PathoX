[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "pathox"
version = "0.1.0"
description = "Gigapixel digital pathology AI engine"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["src"]
